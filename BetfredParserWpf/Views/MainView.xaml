<Window x:Class="BetfredParserWpf.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        xmlns:vm="clr-namespace:BetfredParserWpf.ViewModels"
        xmlns:c="clr-namespace:BetfredParserWpf.Commands"
        Title="Main Window"
        Height="400"
        Width="800">

    <Window.Resources>
        <!-- Allows a KeyBinding to be associated with a command defined in the View Model  -->
        <c:CommandReference x:Key="ExitCommandReference"
                            Command="{Binding ExitCommand}" />
    </Window.Resources>
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Window.InputBindings>
        <KeyBinding Key="F4"
                    Modifiers="Alt"
                    Command="{StaticResource ExitCommandReference}" />
    </Window.InputBindings>

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Файл">
                <MenuItem Command="{Binding ExitCommand}"
                          Header="В_ыход"
                          InputGestureText="Alt-F4" />
            </MenuItem>
        </Menu>

        <Grid>
            <!-- Add additional content here -->
        </Grid>
    </DockPanel>
</Window>
